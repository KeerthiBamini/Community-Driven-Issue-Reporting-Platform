PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/Community

# Use a long random value (32+ chars). Keep it secret.
JWT_SECRET=replace_with_a_long_random_secret_key
JWT_EXPIRES_IN=7d
JWT_ISSUER=community-issues-api
